// manipulasi array 

// menambahkan nilai array 
    // var arr =[];
    // arr[0] = 'fauzan';
    // arr [1] = false;
    // arr [2] = 'mufid';

    // console.log(arr[2]);

// menghapus isi array
    // var arr =['fauzan','mufid',1,false];
    // arr[2] = undefined;
    // console.log(arr);
// menampilkan isi array dengan menggunakan looping

    // var arr =['fauzan','mufid','galih','sandkka'];

    // for(var i = 0; i< arr.length; i++){
    //     console.log((i+1) + arr[i]);
    // }

// method pada array
// 1. join
        // push & pop
        // push = memasukkan nilai di akhir array
        // pop = menghapus nilai akhir array
var arr =['fauzan','mufid','galih','sandkka'];
arr.push('mufidd');
arr.pop();
        // shift dan unshift
        // shift menghapus nilai awal array, unshift menambahkan nilai awal array
arr.shift();
arr.unshift('sandika');
console.log(arr.join(' '));


























